import { ColorConfig } from "@/types";
import { palette } from "../palette";

export const cyanIds: ColorConfig = {
  color: palette.cyan,
  targets: ["terminal.ansiBrightCyan", "terminal.ansiCyan"],
};
