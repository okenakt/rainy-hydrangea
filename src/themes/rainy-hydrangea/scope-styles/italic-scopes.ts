import { StyleConfig } from "@/types";

export const italicScopes: StyleConfig = {
  style: "italic",
  targets: [
    /* Target Scopes 
      - comment
     */
    "comment",
    "punctuation.definition.comment",
  ],
};
